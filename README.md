# IP Sweep Script

A simple Bash script to perform host discovery on a local network.

## 📌 Description
This script sends one ICMP packet to each IP in a subnet range (1–254)
and prints active hosts.

## ⚙ Usage

```bash
chmod +x ipsweep.sh
./ipsweep.sh 192.168.1
